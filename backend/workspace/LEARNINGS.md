# Brain OS — Learnings
> Auto-generated by Brain OS self-learn engine

---
**[14:34:15 16/3/2026]** `HIGH` · `ui`

User preference: Always display Monday.com or any list data as a single HTML <table> inside a scrollable container (e.g., <div style='overflow-x:auto'>). Do not use Markdown tables or plain text for Monday data.

*Tags: user_preference*

---
**[16/3/2026]** `CRITICAL` · `tool`

Monday.com integration: NEVER use mcp_call for fetching Monday data. ALWAYS use http_request tool with POST to https://api.monday.com/v2. Headers required: Authorization: <TOKEN> (no Bearer prefix), Content-Type: application/json, API-Version: 2023-10. Body must be JSON with "query" and "variables" fields.

*Tags: best_practice, tools, monday*

---
**[16/3/2026]** `CRITICAL` · `tool`

Monday.com GraphQL: use column_values { id title text value type } — the "text" field is already human-readable (e.g. "Done", "Alice"). Only parse "value" (raw JSON string) when "text" is empty. Never use inline GraphQL fragments (... on StatusValue) — they are not supported in this API version.

*Tags: best_practice, tools, monday*

---
**[16/3/2026]** `HIGH` · `tool`

Monday.com workflow order: (1) GET groups to find group_id, (2) GET columns to find column ids if needed, (3) GET users to find user ids if filtering by person, (4) GET items with filter. Board ID is always Int, Group ID is always String.

*Tags: best_practice, monday*
---
**[23:19:13 16/3/2026]** `HIGH` · `config`

monday_schema_board_5027230907: status_col=task_status|Status, people_col=task_owner|Owner, numbers_col=task_estimation|Estimated SP, date_col=—

*Tags: fact*

---
**[09:08:44 17/3/2026]** `CRITICAL` · `api`

Monday board 5027230907: WORKING query to get items = boards(ids:[$b]){groups{items_page(limit:100){cursor items{id name created_at updated_at group{id title}column_values{id text value type}}}}}. Variables: {b:\"BOARD_ID\"}. CONFIRMED WRONG patterns: boards{items{}} = error 'Cannot query field items on Board'. boards{groups{items{}}} = error 'Cannot query field items on Group'. boards{groups{items_page{}}} without limit = works. items_page IS inside groups, NOT directly on boards. column_values uses ONLY {id text value type}, no title field.

*Tags: fact, tools*

---
**[10:47:33 17/3/2026]** `CRITICAL` · `api`

Monday MCP CRITICAL: get_board_items_by_name does NOT return column_values or subitems. Must use all_monday_api tool. Always filter column_values(ids:[specific_ids_only]) to avoid complexity exhaustion. Fetch subitems in a separate all_monday_api call after getting parent item IDs. retry_in_seconds in complexity error tells how long to wait.

*Tags: fact, tools*

---
**[11:12:29 17/3/2026]** `CRITICAL` · `api`

Monday MCP CRITICAL: get_board_items_by_name omits column_values. Use all_monday_api with items_page query. Filter column_values(ids:[STATUS_ID,PERSON_ID,NUMBERS_ID]) — never fetch all columns. Subitems require separate all_monday_api call with items(ids:$ids){subitems{...}}. Brain calls mcp_call directly — no roundtrip through mcp-monday agent for data fetching.

*Tags: fact, agents*

---
**[11:12:32 17/3/2026]** `HIGH` · `api`

Monday board 5027230907: working items_page query uses groups{items_page(limit:50){cursor items{id name created_at updated_at group{id title}column_values(ids:["COLS"]){id text value type}}}}. Get column IDs first via get_board_schema.

*Tags: fact*

---
**[12:08:51 17/3/2026]** `CRITICAL` · `api`

Monday data fetching: Use http_request POST https://api.monday.com/v2 with Authorization token from get_monday_token(). Working query: {query:"query($b:ID!){boards(ids:[$b]){columns{id title type}groups{id title items_page(limit:50){cursor items{id name state created_at updated_at group{id title}column_values{id text value type}}}}}}",variables:{b:"BOARD_ID"}}. Then fetch subitems separately: {query:"query($ids:[ID!]!){items(ids:$ids){id name subitems{id name column_values{id text value type}}}}",variables:{ids:[...]}}. Use the raw token value in the Authorization header (no 'Bearer' prefix).

*Tags: fact*

---
**[12:08:53 17/3/2026]** `CRITICAL` · `ui`

Monday display: ALWAYS render HTML immediately. NEVER ask user 'bạn muốn xuất dạng nào?'. After fetching items+subitems, immediately call mcp-monday agent to format as HTML table. Parent rows + subitem rows interleaved (subitems have ↳ prefix, indent 28px). Status colored dot with exact API label text.

*Tags: fact, agents*

---
**[12:09:42 17/3/2026]** `LOW` · `config`

Monday integration: FULL RESET performed — lessons removed, agents recreated, Brain updated to use http_request, and MCP server reconnected. (This is an audit record)

*Tags: routing, agents*

---
**[19:51:17 17/3/2026]** `CRITICAL` · `api`

Monday complexity fix: NEVER put column_values inside items_page/boards query — mirror(lookup) and board-relation columns multiply complexity beyond budget. ALWAYS use 3 separate queries: (1)items_page with NO column_values to get IDs cheaply, (2)items(ids:[batch10]){column_values(ids:[safe_ids])} for column data, (3)items(ids:all){subitems{}} for subitems. Safe column IDs for board 5027230907: task_status, task_owner, task_estimation. AVOID in ids filter: mirror, mirror__1, task_epic, task_sprint, monday_doc_v2 (these are lookup/board-relation types).

*Tags: fact*

---
**[19:51:21 17/3/2026]** `HIGH` · `api`

Monday board 5027230907 structure: subitemsBoardId=5027239243. Groups vary (Sprint 1, Backlog, etc). Status column=task_status(color type), owner=task_owner(multiple-person), estimate=task_estimation(numeric). column_values text field returns human-readable label directly — no need to map index to label.

*Tags: fact, files*

---
**[20:34:45 17/3/2026]** `CRITICAL` · `api`

Monday complexity ROOT CAUSE: boards{groups{items_page(limit:N)}} multiplies complexity by number_of_groups × N → explodes with many sprints. CORRECT: boards{items_page(limit:50)} is linear, no group multiplier. Always use boards.items_page directly, then paginate with next_items_page(cursor). Then fetch columns separately via items(ids:[batch10]). Never nest items_page inside groups.

*Tags: fact*
